# WORK ONLY — сохранённый контекст после baseline 150/150

Диапазон: **8 000–15 000 ₽ включительно**, вся Россия, настольные компьютеры, сортировка новые сначала (`s=104`). Нагрузка: ChatGPT, много вкладок, CorelDRAW, офис, документы, многозадачность. Приоритет: **CPU → RAM → SSD/NVMe → платформа / надёжность / апгрейд → GPU**.

## Baseline

Новый baseline 8–15 тысяч завершён и **не пересобирается**:

- `пк = 50/50`;
- `компьютер = 50/50` (`p1=30`, первые 20 уникальных `p2`; p3 не нужен);
- `системный блок = 50/50` (`p1=30`, первые 20 уникальных `p2`; p3 не нужен);
- всего `150/150` query-позиций.

После baseline завершён первый detail-пакет из шести лотов. Он не дал достаточного основания автоматически менять ACTIVE WORK TOP.

## Последний подтверждённый TOP до обязательного POST-revalidation

1. Dell Vostro 3470, ID `4750223208`: i5-8400 / 16 GB / SSD256, 10 000 ₽; ранее прямая «Купить с доставкой» была подтверждена.
2. Mini ITX, ID `8156773014`: i5-8400T / 16 GB / M.2 SSD256, 13 950 ₽; ранее прямая доставка была подтверждена.

Это **предыдущее подтверждённое состояние**, а не новый live-pass 2026-09-10. Две попытки POST-revalidation остановились до первого чтения из-за `AVITO_RATE_LIMIT`, поэтому TOP по ним не менялся.

## Постоянный WORK-эталон

Lenovo IdeaCentre 90HT001NRS: i7-8700 / 16 GB DDR4 / SSD480 + HDD500 / 13 499 ₽. Это историческая постоянная точка сравнения и меняется только по явному решению пользователя.

## Текущий блокирующий live-сигнал

POST-revalidation:

- попытка 1: `AVITO_RATE_LIMIT`, `cursor=0/7`, `attempted=0`;
- попытка 2: `AVITO_RATE_LIMIT`, `cursor=0/3`, `attempted=0`.

Ни одна пустая строка «без названия / цена не найдена» из этих остановленных очередей **не является прочитанным лотом**.

## Следующий шаг после установки v1.0.28

Не запускать baseline. Повторить `TOP_REVALIDATE_POST / batch 1` только для:

- Dell Vostro 3470 `4750223208`;
- Mini ITX `8156773014`;
- Lenovo M715s `8375159229`.

Finder v1.0.28 должен самостоятельно обработать 429/видимую rate-limit страницу через bounded cooldown + durable wake, не продвигая cursor и не отправляя фиктивный отчёт до фактического чтения или терминального исчерпания recovery.

ACTIVE-критерий не меняется: живой публичный URL, цена 8–15k, полная исправная машина, читаемый непротиворечивый паспорт и **видимый включённый интерактивный элемент с точным текстом «Купить с доставкой»**. «Доставка от…», «Скидка на доставку», «Авито Доставка», «В корзину», icebreaker и текст продавца сами по себе не подтверждают direct delivery.
