# Текущий checkpoint проекта Avito/Комп

Дата фиксации: **2026-09-12**.

Это recovery-point для продолжения работы. Старый checkpoint v1.0.24 заменён как устаревший.

## 1. WORK-поиск ПК

Сохранённый baseline текущего рабочего цикла:

- `пк = 50/50`;
- `компьютер = 50/50`;
- `системный блок = 50/50`;
- **ИТОГО = 150/150**.

Baseline не пересобирать без отдельного решения. Следующий поиск должен работать как fresh CURRENT_50 → diff с сохранённым OLD_50 → detail только новых сильных кандидатов → TOP_REVALIDATE_POST.

Текущий лидер поиска, который нельзя исключать только из-за долгой доставки:

- Dell Vostro 3470, ID `4750223208`, i5-8400 / 16 GB / SSD 256 GB / 10 000 ₽.

Постоянный исторический WORK-эталон:

- Lenovo IdeaCentre, i7-8700 / 16 GB / SSD 480 GB / HDD 500 GB / 13 499 ₽.

Пользовательское решение не покупать конкретный лот из-за срока доставки не меняет объективные критерии TOP.

## 2. Последний живой дефект расширения

После validator-error assistant выдал ordinary Markdown/code block. Generic assistant-turn Copy уже подтверждал завершение turn, но Writing Block отсутствовал. Exact v1.0.34 правильно запрещал исполнять ordinary code block, однако capture state machine не имел terminal outcome и мог бесконечно оставаться в `PROMPT_WAIT_WRITING_BLOCK`.

## 3. Exact source baseline

Owner-supplied exact source:

`AVITO_FINDER_v1.0.34_WRITING_BLOCK_BODY_STABILITY_2026-09-12(1).zip`

SHA-256:

`27e2049522663bc3d2925c7bc847ad04304979e166e8428fafaaa91d37085f5d`

Manifest:

- version `1.0.34`;
- version_name `1.0.34-writing-block-body-stability`.

## 4. Текущий patch candidate

`AVITO_FINDER_v1.0.35_ASSISTANT_FORM_TERMINAL_GATE_2026-09-12.zip`

SHA-256:

`3a1de9f67af1554038193adb9bf6a63f0e7ca5ef7c26690236e2fd57a287abd3`

Manifest:

- version `1.0.35`;
- version_name `1.0.35-assistant-form-terminal-gate`;
- capture protocol `0.6.18`.

Исправление:

- Writing Block остаётся единственной executable assistant-командой;
- ordinary Markdown/code block остаётся zero-command;
- если assistant-turn уже финален, но Writing Block отсутствует, Finder возвращает один terminal same-chat report `ASSISTANT_WRITING_BLOCK_REQUIRED` вместо бесконечного ожидания;
- Avito navigation/mutation в этом случае не выполняется;
- validation wording разделяет ordinary user message и executable assistant response.

## 5. RED → GREEN / QA

Exact v1.0.34:

- command_count = 0;
- form_error_count = 0;
- **RED / FAIL**.

Patched v1.0.35:

- command_count = 0;
- form_error_count = 1;
- **GREEN / PASS**.

Уже выполнено локально:

- runtime JS syntax: PASS;
- focused RED→GREEN live-DOM fixture: PASS;
- Writing Block block01: 12/12 PASS;
- parser block02: 12/12 PASS;
- worker payload block03: 8/8 PASS;
- Node regression: 347/347 PASS;
- Avito DOM: 16/16 PASS;
- ChatGPT DOM: 11/11 PASS;
- final ZIP rebuilt and SHA regenerated.

Полный длинный aggregate run не завершён целиком из-за tool timeout. Installed/live E2E ещё НЕ пройден.

## 6. GitHub authority

Постоянные правила патчей:

`подбор авито расширение/PATCH_ENGINEERING_RULES.md`

Отдельный подробный recovery checkpoint:

`подбор авито расширение/CHECKPOINT_2026-09-12_v1.0.35.md`

Main содержит materializer workflows для v1.0.34 и v1.0.35.

Закрытый PR #1 от v1.0.24 — **INVALID BASELINE**, не использовать и не merge.

## 7. Точная точка остановки

Работа остановлена НЕ на анализе дефекта и НЕ на создании patch logic. Они уже сделаны.

Следующие действия строго такие:

1. Materialize exact v1.0.34 authority в GitHub.
2. Materialize exact v1.0.35 source + installable ZIP + patch report + SHA metadata в GitHub.
3. Remote readback и hash/version verification.
4. Только после этого обновить `АКТУАЛЬНАЯ_ВЕРСИЯ_РАСШИРЕНИЯ.md` на v1.0.35 candidate/current authority.
5. Установить тот же v1.0.35 ZIP в пользовательский Chrome.
6. Прогнать полный live E2E от assistant response до следующего prompt.
7. Любой live FAIL = версия NOT READY; следующий патч запрещён до разбора FAIL.

## 8. Запрет на потерю состояния

После каждого материального блока работы немедленно создавать GitHub commit/checkpoint. Не оставлять единственную копию результата в чате, sandbox или локальном ZIP.