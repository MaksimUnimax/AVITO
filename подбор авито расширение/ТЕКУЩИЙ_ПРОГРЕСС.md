# Текущий прогресс Avito/Комп

Дата актуализации: 2026-09-13.

## Текущая точка — LIVE FAIL v1.0.37

Установленная сборка по отчёту Finder: **1.0.37**.

Последний живой запуск:

- task: `af-20260913040603-4j0t`;
- plan: `575:97fe6009`;
- stage: `TOP_REVALIDATE_PRE`;
- status: **`AVITO_IP_BLOCK_RECOVERY_EXHAUSTED`**;
- explicit queue: `3` лота;
- cursor: `0/3`;
- attempted: `0`;
- публичных карточек прочитано: `0`;
- remaining: `3`;
- step status: `paused_for_connection`.

Точный пользовательский отчёт нормализован и сохранён в:

`releases/v1.0.37/LIVE_ACCEPTANCE_FAIL_2026-09-13_af-20260913040603-4j0t.json`

**Вывод:** v1.0.37 прошла offline/CI/remote-byte gates, но **не прошла installed live acceptance на Avito**. Статус релиза теперь:

`OFFLINE_QA_PASS / REMOTE_BYTES_VERIFIED / INSTALLED_LIVE_FAIL__IP_BLOCK_RECOVERY_EXHAUSTED`

Нельзя называть v1.0.37 рабочей на живом Avito. Нельзя повторять поиск/мутации вслепую после exhaustion. Сначала нужен exact diagnostic state этого запуска. Следующий patch запрещён без нового RED на exact installed v1.0.37.

## Сохранённая очередь — не сбрасывать

1. Dell Vostro 3470 — ID `4750223208`;
2. Mini ITX i5-8400T — ID `8156773014`;
3. Lenovo M715s — ID `8375159229`.

Очередь не изменена. Непрочитанные карточки не считать проверенными.

## Baseline и ranking — не менять

Baseline сохранён:

- `пк = 50/50`;
- `компьютер = 50/50`;
- `системный блок = 50/50`;
- всего `150/150`.

Ranking criteria не менялись. Долгая доставка Dell сама по себе не исключает его из TOP.

После восстановления соединения продолжать тот же pipeline:

`TOP_REVALIDATE_PRE → CURRENT_50 по трём прежним запросам → diff OLD_50 → detail новых сильных кандидатов → TOP_REVALIDATE_POST`.

## v1.0.37 release authority

Installable ZIP:

`releases/v1.0.37/AVITO_FINDER_v1.0.37_PROXY_RECOVERY_INTEGRITY_2026-09-13.zip`

- SHA-256: `4eaf64038ffd6c5b08ed7d97fdb0d13848edd8a7a9db04ad928f74b1f371ac9d`;
- bytes: `488986`;
- source files: `144`;
- worktree QA: `35/35 PASS`;
- final-ZIP fresh-extract QA: `35/35 PASS`;
- Node aggregate: `367/367 PASS`;
- independent remote readback: PASS;
- installed live acceptance: **FAIL** по текущему запуску.

Authority:

- `releases/v1.0.37/PATCH_REPORT_v1.0.37_RU.md`;
- `releases/v1.0.37/MAIN_RELEASE_RECEIPT.json`;
- `releases/v1.0.37/REMOTE_READBACK.json`;
- `releases/v1.0.37/PRE_PATCH_HISTORY_REVIEW.md`;
- `PATCH_ENGINEERING_RULES.md`.

## Что уже исправлялось в v1.0.37

v1.0.37 исправляла доказанные ошибки telemetry/state вокруг proxy recovery: exact PAC ownership/match, единый `probe_before/probe_after` contract, разделение transport probe и Avito target, current attempt receipt перед CAPTCHA, durable probe errors, same-profile auth evidence, mixed IP-block+CAPTCHA display, manual CAPTCHA gate staging, явный endpoint-create skip reason и stale key indicator.

Это **не доказало**, что bounded recovery способен получить пригодный Avito egress в текущей живой конфигурации. Новый live FAIL именно это теперь требует исследовать.

## Не трогать без отдельного RED

Сохраняются исторические anti-regression границы:

- v1.0.7 / v1.0.31 exact-target sequential navigation;
- v1.0.32 report reconciliation без blind resend;
- v1.0.33 Writing Block-only execution и manual CAPTCHA;
- v1.0.34 body stability / validation-before-ownership;
- proxy остаётся transport layer;
- `chatgpt_content.js`, queue/cursor и search methodology не менять ради сетевого симптома.

Исторические FAIL не стирать: v1.0.35 version mismatch; v1.0.36 R1 test-VM cleanup; v1.0.36 R2 popup timeout; v1.0.37 workflow harness fail и popup timeout attempt 1; все сохранены отдельно.

## Следующий шаг

Только диагностика текущего exhaustion:

1. снять exact current DOM state;
2. получить текущий proxy diagnostic/recovery receipt из v1.0.37;
3. установить: effective PAC, auth evidence, `probe_before`, `probe_after`, probe errors, attempt/create result, target status и CAPTCHA/IP-block flags;
4. не объявлять смену IP без измеренного evidence;
5. после доказанной причины — RED на exact v1.0.37 → минимальный patch → полный regression → final ZIP → installed live retest.
