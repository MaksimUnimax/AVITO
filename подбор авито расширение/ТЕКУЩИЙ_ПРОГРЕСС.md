# Текущий checkpoint поиска

Дата фиксации: 2026-09-08.

Область: **WORK ONLY**.

## Текущий прогресс нового baseline 8 000–15 000 ₽

- `пк = 50/50` — завершено;
- `компьютер = 0/50` — карточки ещё не собраны;
- `системный блок = 0/50` — не начато.

Последний live-run:

`компьютер / p1` на Avito Finder v1.0.23 дошёл до collection и вернул `COLLECT_LISTINGS: blocked — AVITO_IP_BLOCK` до получения карточек.

Это не новый baseline и не повод сбрасывать `пк = 50/50`.

Причина в расширении найдена: v1.0.23 запускала VERIFIED EGRESS recovery для IP-block на readiness/main-frame этапе, но не для `AVITO_IP_BLOCK`, впервые обнаруженного внутри collection-first UI-plan.

Исправление: **Avito Finder v1.0.24 — UI-PLAN IP-BLOCK RECOVERY**.

Следующее действие после установки/перезагрузки v1.0.24:

**повторить тот же `компьютер / p1`**

После успешного p1:
1. собрать `компьютер / p2`;
2. дедуплицировать по listing ID;
3. получить `компьютер = 50/50`;
4. перейти к `системный блок`;
5. завершить `системный блок = 50/50`;
6. только после полного нового 3×50 считать baseline 8–15k установленным.

Не сбрасывать уже собранный `пк = 50/50` и не начинать новый цикл заново после proxy recovery.

## Важная граница baseline

Старый baseline 0–15 000 ₽ хранится только как история и для распознавания ранее виденных ID.

Он **не является авторитетным OLD_50** для NEW-diff после перехода на диапазон 8 000–15 000 ₽.

Первый полностью завершённый 3×50 с новым price-filter создаёт новый baseline.

## Recovery

Актуальная версия Finder: v1.0.24.

При `AVITO_IP_BLOCK` в collection-first UI-plan Finder должен войти в существующий VERIFIED EGRESS recovery, сохранить bounded attempt counter, reload Avito только после подтверждённого recovery и повторить тот же collection-first план.

CAPTCHA остаётся ручной границей: `CAPTCHA_MANUAL_REQUIRED`.
