# Текущий прогресс Avito/Комп

Дата: 2026-09-12. Актуальная точка поиска: `live_reports/af-20260912140042-p4kl.json`. Точка готовности пакета: `releases/v1.0.36-r2/DELIVERY_CHECKPOINT.md`.

## Последний живой результат — требуется ручная CAPTCHA

После команды пользователя «Ищи» подготовлена перепроверка сохранённых кандидатов. Определение очереди и команды находится в `SEARCH_RESUME_2026-09-12_AFTER_V136_R2.json`.

Получен отчёт Finder, задача **af-20260912140042-p4kl**:

- этап `AVITO_NAVIGATION`;
- причина `CAPTCHA_MANUAL_REQUIRED`;
- автоматических попыток восстановления: 1;
- повтор мутаций запрещён;
- сохранённый курсор очереди: «не используется»;
- карточек прочитано: **0**;
- версия в отчёте: `1.0.36`;
- отчёт вернулся в тот же чат; Finder сообщает ожидание следующей формы.

Исходный текст отчёта и его происхождение сохранены без подмены в `live_reports/af-20260912140042-p4kl.json`, commit `a863d7a910588f94bd0b144c173c8474da4cd064`; файл прочитан обратно. Источник — сообщение Finder, переданное пользователем. Независимого осмотра его вкладки в этом проходе не было.

Текущий статус: **TOP_REVALIDATE_PRE_PAUSED_FOR_MANUAL_CAPTCHA**. Все три запрошенных лота (4750223208, 8156773014, 8375159229) остаются непрочитанными в этом запуске. Очередь сохранена в Git как определение задания; наличие рабочего cursor у расширения не подтверждено. Нельзя подменять «не используется» утверждением «cursor=0 сохранён».

### Следующий ручной шаг

На время ручной проверки пользователь нажимает «Стоп» в Finder, затем проходит CAPTCHA в существующей вкладке Avito и сообщает, что обычная страница объявления открылась. Эти ручные действия пока НЕ подтверждены. После подтверждения продолжить перепроверку той же сохранённой очереди с учётом фактического состояния Finder, не предполагать существующий runtime cursor и не переходить сразу к новому 3×50.

До ручного подтверждения не выдавать исполняемую форму, не повторять навигацию или мутации, не переключать proxy/IP и не решать CAPTCHA автоматически. В этом ответе новые Avito-команды не выдавались; код, ZIP и настройки не изменялись.

Для инструкции ручной остановки проверен исходник опубликованного R2 ZIP с SHA-256 `c703c9875687d69b2d2ab5e265268cb5fd9ef44d86a95c305a6ae24e4589104c`: popup.html содержит кнопку «Стоп», service_worker.js:3677–3691 сохраняет CANCELLED_BY_USER/STOPPED и останавливает capture; этот обработчик не очищает хранилище и не удаляет исходное определение очереди. Это проверка кода пакета, не доказательство нажатия кнопки пользователем.

### Граница доказанного

Отчёт подтверждает наблюдение остановки на навигации и возврат сообщения. **Успешного сбора и полного пользовательского E2E нет.** Версия 1.0.36 сама по себе не подтверждает точный ZIP/R2; хеш установленной копии неизвестен. Приёмка: **BLOCKED_MANUAL_CAPTCHA / LIVE_UNVERIFIED**, не PASS. Причина появления CAPTCHA и отдельный дефект расширения этим отчётом не установлены; не делать новый патч по предположению. Смена IP не подтверждена.

## Сборка и публикация завершены ранее

Пакет: `releases/v1.0.36-r2/AVITO_FINDER_v1.0.36_PACKAGED_ADAPTER_CONSISTENCY_R2_2026-09-12.zip`.

SHA-256: `c703c9875687d69b2d2ab5e265268cb5fd9ef44d86a95c305a6ae24e4589104c`; 481259 байт; 141 файл исходников в `releases/v1.0.36-r2/v136_work/`.

Статус опубликованного пакета: **OFFLINE_QA_PASS / REMOTE_BYTES_VERIFIED / LIVE_UNVERIFIED**. ZIP R2 не пересобирался и не изменялся.

## Подтверждённые результаты сборочного цикла

CI 34696794119: исходный полный runner из свежей распаковки опубликованного ZIP — 35 групп PASS / 0 FAIL. Внутри Node-набора 351 тест PASS; это не замена межкомпонентному сценарию. Popup: 31/31 в полном прогоне и три дополнительных повтора по 31/31 с прежним лимитом 30 секунд.

Полный offline переход: обычный Markdown → один same-chat ASSISTANT_WRITING_BLOCK_REQUIRED без выполнения команды → две Writing Block-команды → два отчёта по 30 тестовых карточек → WAITING_FOR_NEXT_ASSISTANT_FORM. В том сборочном цикле живых provider calls не было.

Отдельный fresh checkout проверил ZIP по SHA-256/CRC и все 141 файла побайтно. Проверен commit `e132cb0159e127d65bd8c5b660053cacd2712daf`; receipt сохранён commit `84962ecb3760f5ef4aaae6dbf50c24c2f4392018`. Artifact 10299590369 скачан обратно; ZIP и Git blob совпали. Доказательства: `releases/v1.0.36-r2/QA/verification_34696794119_1/` и `DELIVERY_RECEIPT.json`.

## Исправление и сохранённая история

В v136 согласованы manifest и настоящий Avito PING: 1.0.36. От exact v135 изменены только manifest.json и константа ADAPTER_VERSION; строгая worker-проверка не ослаблена. Все прочие production-файлы прежние. R2 исправляет только очистку offline test VM после dispose; production R1/R2 побайтно одинаков.

v135 остаётся отклонённой. R1 v136 и его тестовый FAIL сохранены. Первый CI R2 (34695322848) — 34 PASS / 1 popup timeout FAIL; лежит отдельно в QA/final_zip/. Его точная причина не установлена. Новые успешные прогоны старый FAIL не отменяют и не объясняют автоматически.

Публикация v134/v135 завершена ранее. Не возвращаться к потерянному транспорту, part-010, старой v124 или закрытому PR #1.

## Сохраняемые контракты и поиск

Writing Block — единственная исполняемая assistant-команда; ordinary Markdown/code block — zero commands. Неверный формат не запускает Avito. CAPTCHA остаётся ручной. Proxy/navigation/queue/cursor/report lifecycle не переписываются ради версий.

Сохранённый baseline: пк 50/50, компьютер 50/50, системный блок 50/50; всего **150/150**. Он не сброшен и не изменён этим проходом. Сохранённый лидер Dell Vostro 3470 ID4750223208, i5-8400/16GB/SSD256GB/10000₽. Исторический эталон Lenovo IdeaCentre i7-8700/16GB/SSD480GB/HDD500GB/13499₽. Новая перепроверка объявлений и цен не состоялась из-за указанной CAPTCHA. Старый TOP не считать заново подтверждённым ACTIVE.

После ручного снятия преграды продолжать TOP_REVALIDATE_PRE; затем CURRENT_50 по прежним трём запросам → diff OLD_50 → новые сильные detail → TOP_REVALIDATE_POST. Прогресс каждого материального блока сохранять в GitHub и читать обратно. При каждом патче показывать все применимые правила и отдельный раздел нарушений; в текущем проходе патча нет.
