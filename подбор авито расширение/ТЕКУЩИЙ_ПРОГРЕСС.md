# Текущий прогресс Avito/Комп

Дата актуализации: 2026-09-13.

## Rule 20 — исторический аудит

`FULL_HISTORY_AUDIT=PASS`.

Authority:

- `PRE_PATCH_FULL_HISTORY_AUDIT_2026-09-13.md`;
- `PRE_PATCH_FULL_HISTORY_AUDIT_2026-09-13.json`;
- `history_audit/2026-09-13/FULL_HISTORY_AUDIT_REMOTE_READBACK.json`;
- `history_audit/2026-09-13/FULL_HISTORY_AUDIT_STATUS.md`;
- Rule 20 в `PATCH_ENGINEERING_RULES.md`.

Охвачены все обнаруженные historical authorities, включая 61 archive variant / 56 уникальных pre-1.0 версий, 60 exact historical runtimes, 54 unambiguous adjacent runtime diffs, reachable Git history, runtime/test/history commits, refs и PR. Source gaps и исторические hash mismatches сохранены явно, без догадочной реконструкции.

## Текущая release authority

**Avito Finder v1.0.39 — Exact Requested Tab Disambiguation**.

PR #5 merged в `main`.

Merge commit:

`8a5c9b9e8333ba617e6420db4352d972e3f6f0fd`

Main release receipt commit:

`51f811987013035c44641c1864aed52829fa3985`

Финальный ZIP:

`releases/v1.0.39/AVITO_FINDER_v1.0.39_EXACT_REQUESTED_TAB_DISAMBIGUATION_2026-09-13.zip`

- SHA-256: `a4a0ae626de4692c62cd76d87be19e34749b488e76afa2764828a025db08bb98`;
- bytes: `492784`;
- source files: `148`;
- targeted exact requested-tab regression: `5/5 PASS`;
- worktree grouped QA: `35 PASS / 0 FAIL`;
- Node aggregate: `369 PASS / 0 FAIL`;
- final ZIP fresh-extract grouped QA: `35 PASS / 0 FAIL`;
- independent fresh-GitHub-checkout readback: `PASS`;
- installed user Chrome E2E: **NOT_RUN**.

Статус:

`MAIN_AUTHORITY / OFFLINE_QA_PASS / REMOTE_BYTES_VERIFIED / LIVE_UNVERIFIED`.

## Живой дефект, из-за которого появился v1.0.39

Installed v1.0.38 task:

`af-20260913062414-29ps`

Дважды остановился до любого Avito-действия:

- stage `COMMAND_CAPTURED`;
- reason `AVITO_VISIBLE_TAB_AMBIGUOUS_SELECT_ONE_TAB`;
- automatic recovery `0`;
- mutations `0`;
- cards read `0`.

При этом assistant command уже содержала exact Dell URL:

`https://www.avito.ru/yuzhno-sahalinsk/nastolnye_kompyutery/dell_vostro_3470_4750223208`

Exact v1.0.38 `ensureAvitoTarget(...)` знал `requestedUrl`, но `selectVisibleAvitoTab(windowId)` его не получал. При нескольких Avito tabs и активной вкладке ChatGPT selector видел 0 active Avito tabs и завершался `AMBIGUOUS`, даже если ровно одна существующая Avito tab уже соответствовала exact requested route.

Live evidence:

`live_evidence/2026-09-13_v138_visible_tab_ambiguity.md`.

## Scope v1.0.39

Production behavior относительно exact v1.0.38 изменён только в `service_worker.js`:

1. исторический приоритет единственной active Avito tab сохранён;
2. если единственной active Avito tab нет, а command содержит `requestedUrl`, Finder сравнивает существующие Avito tabs через уже существующий `sameVisibleAvitoRoute(...)`;
3. **ровно один** exact normalized requested-route match может быть выбран как `exact_requested_avito_tab`;
4. `0` exact matches при нескольких Avito tabs остаётся terminal `AMBIGUOUS`;
5. `>1` exact matches остаётся terminal `AMBIGUOUS`;
6. tab не навигируется лишь ради разрешения неоднозначности;
7. прежнее single-Avito-tab behavior сохранено.

Release identity изменён также в:

- `manifest.json` → `1.0.39`;
- `avito_content.js` adapter identity → `1.0.39`.

Побайтно неизменны относительно v1.0.38:

- `chatgpt_content.js`;
- `core.js`;
- `proxy_manager.js`;
- `recovery.js`;
- `popup.js`;
- `popup.html`;
- `popup.css`.

Не менялись queue/cursor semantics, target navigation dispatch, proxy/recovery policy, CAPTCHA boundary, Writing Block contract, ChatGPT capture или report delivery.

## RED → GREEN

Перед runtime patch exact v1.0.38 был проверен permanent regression:

- multiple Avito tabs;
- ChatGPT active, поэтому active Avito = none;
- ровно одна существующая tab соответствует exact normalized Dell requested route;
- v1.0.38 → `AVITO_VISIBLE_TAB_AMBIGUOUS_SELECT_ONE_TAB` = RED.

v1.0.39:

- unique exact requested route → selected;
- zero exact matches → AMBIGUOUS;
- duplicate exact matches → AMBIGUOUS;
- unique active Avito preference → preserved;
- single Avito tab → preserved;
- targeted result `5/5 PASS`.

## Первый full gate — сохранить как FAIL

Workflow `34743154320` завершился `33 PASS / 2 FAIL` и не является успешным релизным gate.

FAIL:

1. новый regression-файл был standalone helper и требовал `argv[2]`, но aggregate `node --test tests/*.test.js` запускает test-файлы без пользовательского аргумента → `SOURCE_PATH_REQUIRED`;
2. `adapter_identity` упёрся в старый 20-second group deadline на `20.003s`.

Runtime после этого FAIL не менялся.

A/B diagnostic workflow `34743603061` доказал:

- v1.0.38 Node aggregate: `368/368 PASS`;
- temporary v1.0.39: единственный FAIL `SOURCE_PATH_REQUIRED` нового test-файла;
- adapter identity на одном runner, одинаковый 20s external limit:
  - v1.0.38: `19.55 / 0.96 / 0.95 / 0.75s`;
  - v1.0.39: `2.39 / 1.20 / 0.77 / 0.89s`.

Отсюда выполнены только test-harness corrections:

- permanent regression при отсутствии argv[2] тестирует соседний release `service_worker.js`;
- adapter identity QA deadline `20 → 30s`, поскольку неизменённая v1.0.38 на cold first launch уже занимала 19.55s. Production timeout не менялся.

Diagnosis сохранён:

`v139_prepatch_tests/FIRST_GATE_FAILURE_DIAGNOSIS.md`.

## Финальный успешный gate

Workflow `34743734524`:

- Rule-20 authority readback: PASS;
- exact v1.0.38 authority/hash/readback: PASS;
- exact v1.0.38 RED: PASS;
- deterministic minimal materialization: PASS;
- targeted GREEN: 5/5;
- worktree full suite: 35/35;
- Node: 369/369;
- final deterministic ZIP: PASS;
- fresh-extract final ZIP full suite: 35/35;
- final ZIP Node: 369/369;
- source/ZIP persistence: PASS;
- independent fresh GitHub checkout byte/hash readback: PASS.

Branch verified commit from readback:

`8248b0075ce99a62ba47136faf0c180c5a8e1d41`.

Authority:

- `releases/v1.0.39/PATCH_REPORT_v1.0.39_RU.md`;
- `releases/v1.0.39/BUILD_v1.0.39.json`;
- `releases/v1.0.39/CHECKPOINT.json`;
- `releases/v1.0.39/MATERIALIZATION.json`;
- `releases/v1.0.39/REMOTE_READBACK.json`;
- `releases/v1.0.39/MAIN_RELEASE_RECEIPT.json`;
- `releases/v1.0.39/v139_work/BUILD_ORIGIN_v1.0.39.json`.

## Сохранённый поиск — НЕ СБРАСЫВАТЬ

Baseline остаётся:

- `пк = 50/50`;
- `компьютер = 50/50`;
- `системный блок = 50/50`;
- total `150/150`.

TOP_REVALIDATE_PRE:

1. Dell Vostro 3470 — ID `4750223208`;
2. Mini ITX i5-8400T — ID `8156773014`;
3. Lenovo M715s — ID `8375159229`.

Последняя v1.0.38 ambiguity task не прочитала ни одной карточки. Никакие результаты нового поиска не добавлены.

## Следующий обязательный шаг

1. установить exact main-authority v1.0.39 поверх существующей распакованной папки, **не удаляя extension и не очищая storage/baseline**;
2. Reload extension в `chrome://extensions`;
3. обновить вкладки ChatGPT и Avito;
4. проверить version `1.0.39`;
5. если Proxy.Market API key потерян из session после reload — загрузить его снова; persistent plaintext storage не добавлять;
6. повторить тот же TOP_REVALIDATE_PRE с тремя сохранёнными URL;
7. при живом FAIL v1.0.39 немедленно отклоняется и следующий patch снова обязан пройти Rule 20 → live observation → exact-source RED → minimal patch → full final ZIP QA → installed E2E.

До успешного installed Chrome E2E **v1.0.39 нельзя называть доказанно рабочей на живом Avito**.
