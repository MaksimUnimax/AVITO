# Текущий прогресс Avito/Комп

Дата: 2026-09-12. Актуальная точка восстановления: `CHECKPOINT_2026-09-12_PUBLICATION_RECOVERY.md` и `PUBLICATION_CURSOR_2026-09-12.json` в этой папке.

## Текущая задача

Завершить прерванную публикацию exact архивов v1.0.34 и отклонённой v1.0.35, прочитать файлы обратно и подтвердить байты. Только затем продолжать исправление расширения от актуальной базы. Это НЕ новый поиск ПК, НЕ возврат к v1.0.24 и НЕ разрешение на установку v1.0.35.

## Фактическая публикация

Перед этим обновлением проверен main `c1babd1d055a7b29de741082b1b98f350f9d1006`.

- В `.github/avito-exact-archives-20260912/` сохранено 11/26 частей: 000–009 и 025.
- Недостающие части: 010–024. Первый файл: `part-010.b64`.
- Полные ZIP и новые каталоги исходников ещё НЕ материализованы в проверенном main tree.
- Оба оригинальных ZIP есть в текущей рабочей среде, CRC и SHA-256 повторно проверены.
- Полный прежний транспортный пакет в текущей среде ещё не восстановлен. Не смешивать его сохранённый префикс с частями другого пакета.

## Версии и статус

v1.0.34 — owner-supplied exact source; ZIP SHA-256 `27e2049522663bc3d2925c7bc847ad04304979e166e8428fafaaa91d37085f5d`.

v1.0.35 — **REJECTED_RUNTIME_VERSION_CONTRACT / NOT READY**, ZIP SHA-256 `3a1de9f67af1554038193adb9bf6a63f0e7ca5ef7c26690236e2fd57a287abd3`.

Полный повторный offline QA уже завершён и сохранён: 9 групп PASS, 15 FAIL, 1 environment probe. См. `QA_v1.0.35_RECHECK_2026-09-12.json`. Не называть этот агрегат «ещё выполняется» и не переносить исторические локальные PASS на весь ZIP.

Свежая парная проверка настоящего adapter PING снова подтвердила: v1.0.34 согласован; у v1.0.35 manifest 1.0.35, Avito adapter 1.0.34. Результат — `ADAPTER_IDENTITY_RECOVERY_2026-09-12.json`. Worker корректно блокирует сбор с `AVITO_ADAPTER_VERSION_MISMATCH_RELOAD_REQUIRED`.

Старые указания «установить v1.0.35 и проверить» отменены результатом QA. Исходный ZIP не переписывать; защиту version mismatch не ослаблять.

## Контракты, которые сохраняются

Writing Block остаётся единственной исполняемой assistant-командой. Ordinary Markdown/code block — zero commands. Terminal form error не должен открывать Avito или исполнять текст обычного ответа. Закрытый PR #1 от v1.0.24 не использовать.

## Поиск ПК — сохранённое состояние, не новый live recheck

Последний зафиксированный baseline: пк 50/50, компьютер 50/50, системный блок 50/50; всего 150/150. ID baseline не пересоздавать по одному этому счётчику и не сбрасывать при восстановлении расширения.

Сохранённый лидер: Dell Vostro 3470, ID 4750223208, i5-8400 / 16 GB / SSD 256 GB / 10 000 ₽. Долгая доставка сама по себе не исключает его из TOP.

Постоянный исторический эталон: Lenovo IdeaCentre i7-8700 / 16 GB / SSD 480 GB / HDD 500 GB / 13 499 ₽.

После исправления и приёмки расширения продолжать TOP_REVALIDATE_PRE → свежий CURRENT_50 по трём прежним запросам → diff с сохранёнными OLD_50 → detail новых сильных кандидатов → TOP_REVALIDATE_POST. В этом восстановлении объявления и цены заново не проверялись.

## Порядок продолжения разработки

Публикация exact v1.0.34/v1.0.35 → remote byte/hash readback → новая минимальная коррекция доказанного version mismatch → настоящая межкомпонентная регрессия → final ZIP retest → installed/live E2E. До последнего шага LIVE_UNVERIFIED.

После каждого материального блока сохранять результат и cursor в GitHub, проверять чтением обратно. Сначала читать `AGENTS.md` и `PATCH_ENGINEERING_RULES.md`. При каждом патче показывать фактическое соблюдение правил и отдельный раздел нарушений.
