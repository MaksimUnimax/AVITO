# Актуальная сборка расширения

Дата: 2026-09-13.

**Avito Finder v1.0.40 — DEDICATED REQUESTED TAB ALLOCATION.**

Статус: **MAIN_AUTHORITY / OFFLINE_QA_PASS / REMOTE_BYTES_VERIFIED / LIVE_UNVERIFIED**. Конечный ZIP и exact source сохранены в GitHub, проверены из fresh extraction и независимого fresh GitHub checkout. Installed owner Chrome E2E ещё не выполнен, поэтому релиз нельзя называть доказанно рабочим на живом Avito.

Каталог: `releases/v1.0.40/`.

ZIP: `AVITO_FINDER_v1.0.40_DEDICATED_REQUESTED_TAB_ALLOCATION_2026-09-13.zip`.

SHA-256: `04f9bdc205eaa3578d462005ea15749184c5f7e744bd24a19c43e5a88bc1e017`.

Размер: `495520` байт. Source files: `150`.

Main merge commit: `91cdd5504392a6509e06b559d68d3576e728263e`.
Main release receipt: `releases/v1.0.40/MAIN_RELEASE_RECEIPT.json`.
Final workflow: `34747966783`.

Проверки:

- Rule-20 full-history gate для v1.0.40: PASS;
- exact v1.0.39 RED: PASS;
- dedicated requested-tab GREEN: `5/5 PASS`;
- prior v1.0.39 selector contract: `5/5 PASS`;
- worktree grouped QA: `35/35 PASS`;
- worktree Node aggregate: `370/370 PASS`;
- final ZIP fresh-extract grouped QA: `35/35 PASS`;
- final ZIP Node aggregate: `370/370 PASS`;
- independent remote byte/hash/source readback: PASS.

Первый full gate `34747402029` остаётся историческим FAIL `34/35`: `adapter_identity` упёрся в старый внешний deadline 30.003s. Same-runner A/B `34747859905` доказал cold v1.0.39 15.718s и v1.0.40 max около 1.883s; после этого изменён только QA-harness deadline 30→45s, production runtime не менялся.

Production runtime относительно v1.0.39 изменён только в `service_worker.js`; `manifest.json` и `avito_content.js` меняют release identity. При первом user-started binding, когда есть явный публичный requested URL и существующие Avito tabs неоднозначны, Finder больше не угадывает существующую вкладку: создаёт отдельную run-owned public Avito tab и маршрутизирует её на requested URL. Queue/cursor, proxy/recovery, report delivery, capture/validator и manual CAPTCHA не менялись.

Установка для live acceptance: существующее расширение не удалять, storage и baseline `150/150` не очищать. Заменить распакованную папку на exact v1.0.40, Reload extension, обновить ChatGPT/Avito, проверить `Версия: 1.0.40` и повторить TOP_REVALIDATE_PRE.
