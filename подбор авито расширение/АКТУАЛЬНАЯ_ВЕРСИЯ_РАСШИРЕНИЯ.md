# Актуальная сборка расширения

Дата: 2026-09-14.

**Avito Finder v1.0.43 — IP RECOVERY EVIDENCE PRESERVATION.**

Статус: **MAIN_AUTHORITY / DIAGNOSTIC_CORRECTIVE_BUILD / OFFLINE_QA_PASS / REMOTE_BYTES_VERIFIED / LIVE_UNVERIFIED**.

Каталог: `releases/v1.0.43/`.

ZIP: `AVITO_FINDER_v1.0.43_IP_RECOVERY_EVIDENCE_PRESERVATION_2026-09-14.zip`.

SHA-256: `593d31edfb136c68a50e4aa45a157c1e3d296f3beb395e0444c5b4de4c4aca3f`.

Размер: `507760` байт. Source files: `156`.

PR: `#9`.
Main merge commit: `41fb015dc0eac11c4713ff7740c3d92ee396cfff`.
RED workflow: `34795377879`.
Final build workflow: `34796154733`.

Проверки:

- Rule20 v1.0.43: `FULL_HISTORY_AUDIT=PASS` + remote readback;
- exact-v1.0.42 terminal-evidence RED: `RED_CONFIRMED` до runtime patch;
- targeted GREEN: `2/2 PASS`;
- worktree grouped QA: `35/35 PASS`;
- final ZIP fresh-extract grouped QA: `35/35 PASS`;
- final ZIP targeted GREEN: `2/2 PASS`;
- independent branch remote readback: `PASS`;
- installed owner Chrome v1.0.43: **NOT_RUN / LIVE_UNVERIFIED**.

## Что произошло на installed v1.0.42

Task `af-20260914010021-tyyl` успешно прошёл Writing Block capture и дошёл до `AVITO_NAVIGATION`, то есть v1.0.42 закрыла старый capture livelock. Далее Mini ITX `8156773014` получил `AVITO_IP_BLOCK_RECOVERY_EXHAUSTED` после 4 bounded attempts. Post-failure DOM остался `AVITO_IP_BLOCK`, concrete CAPTCHA отсутствовала, карточка не была прочитана, baseline `150/150` сохранён.

## Что исправляет v1.0.43

v1.0.42 уже измеряла recovery telemetry, но terminal failure report её отбрасывал. v1.0.43 добавляет в отчёт:

- IP probe before/after;
- подтверждена ли смена IP;
- transport status и probe errors;
- endpoint/create state, attempt, reason;
- provider rotation state/attempt без ложного заявления IP change.

Поведенчески изменён только terminal formatter в `service_worker.js`. **Сам recovery algorithm не изменён.**

Byte-identical к v1.0.42:

- `chatgpt_content.js`;
- `core.js`;
- `proxy_manager.js`;
- `recovery.js`.

Capture version остаётся `0.6.19`.

## Важная граница

v1.0.43 **не исправляет** `AVITO_IP_BLOCK_RECOVERY_EXHAUSTED`. Это диагностическая corrective build, необходимая для доказательства точного failing substep перед следующим behavior patch.

Первый v1.0.43 build сохранился как FAIL из-за внешнего 30-second timeout `popup_lifecycle`. Отдельный baseline-vs-candidate diagnostic доказал, что production popup не менялся и v1.0.42 baseline сам занимает 29.608s; затем изменён только test-harness outer deadline 30→45s. Финальный build прошёл полностью.

## Установка и следующий live gate

Существующее расширение не удалять. Storage, proxy profiles и WORK baseline `150/150` не очищать.

Установить exact main v1.0.43 поверх существующей unpacked extension, выполнить Reload в `chrome://extensions`, обновить ChatGPT и Avito, убедиться, что Finder сообщает `Версия: 1.0.43`.

Затем повторить **только Mini ITX `8156773014` один раз**. Если exhaustion повторится, новый terminal report должен сам вернуть recovery telemetry. После этого blind retry запрещён: по этим данным определяется causal root cause и только тогда разрешается следующий recovery-behavior patch.
