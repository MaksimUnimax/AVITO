--- v1.0.37/avito_content.js
+++ v1.0.38/avito_content.js
@@ -2,7 +2,7 @@
 /* global chrome, AvitoFinderCore */
 (function avitoContent() {
   "use strict";
-  const ADAPTER_VERSION = "1.0.37";
+  const ADAPTER_VERSION = "1.0.38";
   const previous = globalThis.__avitoFinderAvitoRuntime;
   if (previous?.version === ADAPTER_VERSION && !previous.disposed) return;
   previous?.dispose?.();

--- v1.0.37/manifest.json
+++ v1.0.38/manifest.json
@@ -2,9 +2,9 @@
   "manifest_version": 3,
   "name": "Avito Finder",
   "short_name": "Avito Finder",
-  "version": "1.0.37",
-  "version_name": "1.0.37-proxy-recovery-integrity",
-  "description": "Avito Finder: bounded, cancellable visible-UI execution, same-chat reports, verified proxy configuration and fresh-document recovery; manual CAPTCHA.",
+  "version": "1.0.38",
+  "version_name": "1.0.38-avito-sticky-recovery",
+  "description": "Avito Finder: bounded visible-UI execution with same-chat reports and sticky residential recovery escalation for Avito; manual CAPTCHA.",
   "minimum_chrome_version": "120",
   "permissions": [
     "tabs",

--- v1.0.37/service_worker.js
+++ v1.0.38/service_worker.js
@@ -1013,13 +1013,13 @@
   const beforeIds=new Set((secret.profiles||[]).map(p=>String(p.id)));
   const request=(url,options)=>Recovery.fetchBounded(globalThis.fetch,url,options,{timeout_ms:10000,signal});
   try {
-    await ProxyCore.createProxyInPackage(request,apiKey,{package_id:packageId,country:profile.country||'ru',rotation:0});
+    await ProxyCore.createProxyInPackage(request,apiKey,{package_id:packageId,country:profile.country||'ru',rotation:-1});
     state=await assertCurrentOperation(state);
     await saveState({...state,connection_recovery:{...state.connection_recovery,create:{...reserved.create,state:'ACKNOWLEDGED'}}});
     // One documented list read. An ambiguous create is never repeated.
     const payload=await ProxyCore.fetchProxyMarketPage(request,apiKey,{package_id:packageId,type:'all',page:1,page_size:50,sort:1});
     const profiles=ProxyCore.normalizeProxyMarketList(payload,{package_id:packageId});
-    const candidate=profiles.find(p=>!beforeIds.has(String(p.id)) && p.rotation_settings?.rotate===0 && p.package_id===packageId);
+    const candidate=profiles.find(p=>!beforeIds.has(String(p.id)) && p.rotation_settings?.rotate===-1 && p.package_id===packageId);
     if(!candidate) throw new Error('PROXY_CREATED_ENDPOINT_RECONCILIATION_REQUIRED');
     state=await assertCurrentOperation(state);
     const all=ProxyCore.dedupeProfileSet([candidate,...(secret.profiles||[])]);

