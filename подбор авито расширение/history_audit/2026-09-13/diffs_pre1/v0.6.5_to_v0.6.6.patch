--- v0.6.5/avito_content.js
+++ v0.6.6/avito_content.js
@@ -1,9 +1,9 @@
-/* Avito Finder v0.6.4 — bounded public diagnostics plus generic visible-UI action plans. */
+/* Avito Finder v0.6.6 — bounded public diagnostics, trusted visible type handoff and dialog-scoped dropdown option selection. */
 /* global chrome, AvitoFinderCore */
 (function avitoContent() {
   "use strict";
-  if (globalThis.__avitoFinderAvitoContentV065) return;
-  globalThis.__avitoFinderAvitoContentV065 = true;
+  if (globalThis.__avitoFinderAvitoContentV066) return;
+  globalThis.__avitoFinderAvitoContentV066 = true;
 
   const Core = AvitoFinderCore;
   const MAX_TREE_NODES = 160;
@@ -293,6 +293,45 @@
     const nearby = textOf(node.closest("[role='dialog'],form,article,section,div") || node, 450);
     return /(?:написать|сообщени|чат\b|продавц|позвон|звонок|купить|оплат|заказ|корзин|оформить\s+достав|брон)/iu.test(`${own} ${nearby}`);
   }
+  function dialogScopedOptionCandidates(text) {
+    const wanted = normalizedMatch(text);
+    if (!wanted) return { ok: false, blocked_reason: "UI_OPTION_TEXT_EMPTY", candidates: [] };
+    const dialogs = Array.from(document.querySelectorAll("[role='dialog']")).filter(visible);
+    // Selection is deliberately possible only from an already visible modal dialog.
+    // This keeps generic text matching away from page cards, navigation and seller UI.
+    if (dialogs.length !== 1) return { ok: false, blocked_reason: dialogs.length ? "UI_OPTION_DIALOG_AMBIGUOUS" : "UI_OPTION_DIALOG_MISSING", candidates: [] };
+    const dialog = dialogs[0];
+    const raw = Array.from(dialog.querySelectorAll("[role='option'],[role='listitem'],li,button,a,[data-marker*='option'],[data-marker*='suggest'],div,span"));
+    const seen = new Set();
+    const matches = [];
+    for (const node of raw) {
+      if (!visible(node) || targetForbidden(node)) continue;
+      const textValue = normalizedMatch(textOf(node, 220));
+      if (textValue !== wanted) continue;
+      // Avoid selecting container copies of the same option. A deepest exact-text
+      // element is the only safe generic fallback when Avito supplies no role/marker.
+      const hasExactVisibleChild = Array.from(node.children || []).some((child) => visible(child) && normalizedMatch(textOf(child, 220)) === wanted);
+      if (hasExactVisibleChild) continue;
+      const explicit = node.matches("[role='option'],[role='listitem'],li,[data-marker*='option'],[data-marker*='suggest'],button,a");
+      const clickTarget = node.closest("button,a,[role='option'],[role='listitem'],li,[data-marker*='option'],[data-marker*='suggest']") || node;
+      if (!visible(clickTarget) || targetForbidden(clickTarget) || clickTarget.disabled || clickTarget.getAttribute("aria-disabled") === "true") continue;
+      if (seen.has(clickTarget)) continue;
+      seen.add(clickTarget);
+      matches.push({
+        element: clickTarget,
+        description: `${clickTarget.tagName.toLowerCase()}${clickTarget.getAttribute("data-marker") ? ` data-marker=${clickTarget.getAttribute("data-marker")}` : ""} text=${textOf(clickTarget, 180)}`.slice(0, 360),
+        score: explicit ? 100 : 70,
+        text: textOf(clickTarget, 180)
+      });
+    }
+    const max = matches.reduce((value, item) => Math.max(value, item.score), 0);
+    const winners = matches.filter((item) => item.score === max);
+    const evidence = matches.slice(0, 8).map((item) => ({ description: item.description, score: item.score }));
+    if (!winners.length) return { ok: false, blocked_reason: "UI_OPTION_NOT_VISIBLE", candidates: evidence };
+    if (winners.length !== 1) return { ok: false, blocked_reason: "UI_OPTION_AMBIGUOUS", candidates: evidence };
+    return { ok: true, target: winners[0], candidates: evidence };
+  }
+
   function resolveVisibleActionTarget(step) {
     const target = step?.target || {};
     const wanted = normalizedMatch(target.value || "");
@@ -456,18 +495,18 @@
       if (step.type === "COLLECT_LISTINGS") {
         const listings = collectPublicListings(Math.max(1, Math.min(Number(step.limit || 10), 30))); result.listings = listings; result.steps.push({ index, type: "COLLECT_LISTINGS", status: "completed", count: listings.length }); continue;
       }
-      if (!["CLICK", "TYPE"].includes(step.type)) { result.ok = false; result.blocked_reason = "UI_PLAN_ACTION_NOT_ALLOWED"; result.steps.push({ index, type: step.type || "?", status: "blocked", blocked_reason: result.blocked_reason }); break; }
-      const resolved = resolveVisibleActionTarget(step);
+      if (!["CLICK", "TYPE", "SELECT_OPTION"].includes(step.type)) { result.ok = false; result.blocked_reason = "UI_PLAN_ACTION_NOT_ALLOWED"; result.steps.push({ index, type: step.type || "?", status: "blocked", blocked_reason: result.blocked_reason }); break; }
+      const resolved = step.type === "SELECT_OPTION" ? dialogScopedOptionCandidates(step.text) : resolveVisibleActionTarget(step);
       if (!resolved.ok) { result.ok = false; result.blocked_reason = resolved.blocked_reason; result.steps.push({ index, type: step.type, status: "blocked", blocked_reason: resolved.blocked_reason, candidates: resolved.candidates }); break; }
       await uiDelay(UI_PLAN_PRE_ACTION_DELAY_MS);
       resolved.target.element.scrollIntoView({ block: "center", inline: "nearest" });
       let applied = false;
-      if (step.type === "CLICK") { resolved.target.element.click(); applied = true; }
+      if (step.type === "CLICK" || step.type === "SELECT_OPTION") { resolved.target.element.click(); applied = true; }
       let typeResult = null;
       if (step.type === "TYPE") { typeResult = await setVisibleControlValue(resolved.target.element, String(step.text || "")); applied = Boolean(typeResult?.ok); }
       if (!applied) { result.ok = false; result.blocked_reason = step.type === "TYPE" && typeResult?.reason === "focus_failed" ? "UI_TARGET_FOCUS_FAILED" : (step.type === "TYPE" ? "UI_DEBUGGER_TYPE_FAILED" : "UI_ACTION_APPLY_FAILED"); result.steps.push({ index, type: step.type, status: "blocked", blocked_reason: result.blocked_reason, target_description: resolved.target.description, failure_detail: typeResult?.detail || typeResult?.reason || null }); break; }
       await uiDelay(UI_PLAN_POST_ACTION_DELAY_MS);
-      result.steps.push({ index, type: step.type, status: "completed", target_description: resolved.target.description, text: step.type === "TYPE" ? "введено" : "", input_strategy: step.type === "TYPE" ? typeResult.strategy : "" });
+      result.steps.push({ index, type: step.type, status: "completed", target_description: resolved.target.description, text: step.type === "TYPE" ? "введено" : (step.type === "SELECT_OPTION" ? String(step.text || "") : ""), input_strategy: step.type === "TYPE" ? typeResult.strategy : "" });
     }
     return result;
   }
@@ -494,6 +533,7 @@
     const links = Array.from(document.querySelectorAll("a[href*='/item/'],a[data-marker*='item']")).filter(visible);
     return { url: location.href, title: document.title, captured_at: new Date().toISOString(), blocked_reason: detectBlock(), input_count: inputs.length, button_count: buttons.length, listing_link_count: links.length, actions_performed: "none_read_only" };
   }
+  if (globalThis.__AF_TEST_EXPORTS) globalThis.__AF_TEST_EXPORTS.optionSelection = { dialogScopedOptionCandidates };
   chrome.runtime.onMessage.addListener((message, _sender, sendResponse) => {
     try {
       if (message?.type === "AF_INSPECT_AVITO_DOM") { sendResponse({ ok: true, snapshot: inspect() }); return; }

--- v0.6.5/chatgpt_content.js
+++ v0.6.6/chatgpt_content.js
@@ -7,10 +7,10 @@
   // runtime context. A version guard that simply returns would then prevent a
   // fresh adapter from being injected into the same tab. New injection must
   // supersede, dispose and replace the previous adapter instead.
-  const CONTENT_SCRIPT_VERSION = "0.6.5";
+  const CONTENT_SCRIPT_VERSION = "0.6.6";
   const CONTENT_SCRIPT_PROTOCOL = "avito_finder_bridge_exact_capture_v1";
   const CONTENT_RUNTIME_KEY = "__AVITO_FINDER_BRIDGE_EXACT_CAPTURE_RUNTIME__";
-  const CONTENT_RUNTIME_ID = `0.6.5:${Date.now()}:${Math.random().toString(36).slice(2)}`;
+  const CONTENT_RUNTIME_ID = `0.6.6:${Date.now()}:${Math.random().toString(36).slice(2)}`;
   const priorRuntime = globalThis[CONTENT_RUNTIME_KEY];
   if (priorRuntime && typeof priorRuntime.dispose === "function") {
     try { priorRuntime.dispose("superseded_by_fresh_content_adapter"); } catch (_) {}
@@ -2070,8 +2070,14 @@
       candidate.payload_extraction_error = localPayload.ok === true ? "" : (localPayload.error || "WRITING_BLOCK_LOCAL_BODY_UNAVAILABLE");
       candidate.poll_generation = generation;
       candidate.form_key = `${candidate.anchor_turn_id || ""}|${candidate.assistant_turn_id || ""}|${localTextFingerprint(candidate.prompt_text || "")}`;
-      if (candidate.payload_extracted !== true) {
-        recordDiagnostic("PROMPT_WAIT_PAYLOAD_EXTRACTION", candidate);
+      if (candidate.payload_extracted !== true || Core.normalizeText(candidate.prompt_text || "").length === 0) {
+        candidate.rejection_reason = "EMPTY_WRITING_BLOCK_PAYLOAD_IGNORED";
+        recordDiagnostic("PROMPT_EMPTY_WRITING_BLOCK_IGNORED", candidate);
+        // An empty renderer shell is not a command and must never create a same-chat
+        // validator report or replace the current anchor. Recheck after the DOM settles.
+        candidateFirstSeen = null;
+        schedulePromptCheck(PROMPT_ACTIVE_RECHECK_MS, false, generation);
+        return;
       }
       const response = await request("AUTOMATION_PROMPT_CANDIDATE", { candidate });
       // The Worker can start the next same-chat wait while this older request is

--- v0.6.5/core.js
+++ v0.6.6/core.js
@@ -1,4 +1,4 @@
-/* Avito Finder v0.6.5 isolated shared pure logic with a generic visible-UI action plan. */
+/* Avito Finder v0.6.6 isolated shared pure logic with bounded visible-UI selection. */
 (function attachAvitoFinderCore(root) {
   "use strict";
   const STORAGE = Object.freeze({
@@ -12,7 +12,7 @@
   const VALID_MODES = new Set(["INSPECT_FILTERS", "SEARCH", "DIAGNOSE_DOM", "AVITO_UI"]);
   // These are generic action verbs, not per-element features. Targets remain
   // visible public controls resolved locally by marker/label/placeholder.
-  const UI_ACTION_TYPES = new Set(["CLICK", "TYPE", "WAIT", "WAIT_FOR", "SNAPSHOT", "COLLECT_LISTINGS"]);
+  const UI_ACTION_TYPES = new Set(["CLICK", "TYPE", "SELECT_OPTION", "WAIT", "WAIT_FOR", "SNAPSHOT", "COLLECT_LISTINGS"]);
   const UI_PLAN_MAX_STEPS = 12;
   const UI_PLAN_MAX_TYPED_TEXT = 160;
   const UI_PLAN_MAX_LISTINGS = 30;
@@ -252,6 +252,10 @@
     const collect = clean.match(/^(?:собери|верни)\s+(?:первые\s+|до\s+)?(\d{1,2})?\s*(?:объявлен|карточк|вариант)/iu);
     if (collect) return { type: "COLLECT_LISTINGS", limit: Math.max(1, Math.min(Number(collect[1] || 10), UI_PLAN_MAX_LISTINGS)), source_line: clean };
     if (/^(?:сними|верни|покажи)(?=\s|$|[.:;]).*(?:dom|html|сним|разметк)/iu.test(clean)) return { type: "SNAPSHOT", scope: firstScope(clean), source_line: clean };
+    const selectOption = clean.match(/^(?:выбери|выберите)(?=\s|$|[.:;])/iu);
+    if (selectOption && quoted[0] && /(?:выпадающ|списк|подсказк|вариант|город)/iu.test(clean)) {
+      return { type: "SELECT_OPTION", text: quoted[0].slice(0, 180), source_line: clean };
+    }
     const click = clean.match(/^(?:нажми|выбери|открой|раскрой)(?=\s|$|[.:;])/iu);
     if (click && (quoted[0] || marker)) return { type: "CLICK", target: marker ? { by: "marker", value: marker } : targetFromText(quoted[0]), source_line: clean };
     return null;

--- v0.6.5/manifest.json
+++ v0.6.6/manifest.json
@@ -2,9 +2,9 @@
   "manifest_version": 3,
   "name": "Avito Finder",
   "short_name": "Avito Finder",
-  "version": "0.6.5",
-  "version_name": "0.6.5-cdp-input-only-debugger-test-candidate",
-  "description": "Отдельный Avito Finder: локальный разбор Copy, anchor-fenced continuous dialogue и generic visible-UI action plans. Для одного подтверждённо видимого TYPE допускается краткий chrome.debugger-сеанс только с CDP Input, без Runtime, Network, cookies или скрытых API.",
+  "version": "0.6.6",
+  "version_name": "0.6.6-key-lifecycle-and-bounded-dropdown-selection-test-candidate",
+  "description": "Отдельный Avito Finder: локальный разбор Copy, anchor-fenced continuous dialogue и generic visible-UI action plans. Для одного подтверждённо видимого TYPE допускается краткий chrome.debugger-сеанс только с CDP Input; выбор возможен только из уже видимого выпадающего списка текущего диалога, без Runtime, Network, cookies или скрытых API.",
   "minimum_chrome_version": "109",
   "permissions": [
     "tabs",

--- v0.6.5/service_worker.js
+++ v0.6.6/service_worker.js
@@ -1,4 +1,4 @@
-/* Avito Finder v0.6.5 — Bridge-trace-equivalent chat handoff plus generic visible-UI action engine with a strict CDP Input-only debugger path for trusted visible TYPE. */
+/* Avito Finder v0.6.6 — bounded visible UI action engine with trusted keyboard lifecycle TYPE and dialog-scoped dropdown selection. */
 "use strict";
 importScripts("core.js");
 const Core = AvitoFinderCore;
@@ -6,7 +6,7 @@
 const LOG_KEY = Core.STORAGE.logs;
 const INSPECTION_KEY = Core.STORAGE.lastInspection;
 const CAPTURE_PROTOCOL = "avito_finder_bridge_exact_capture_v1";
-const CAPTURE_VERSION = "0.6.5";
+const CAPTURE_VERSION = "0.6.6";
 const AVITO_READY_TIMEOUT_MS = 30000;
 // Default production pauses are deliberately visible: the user can observe an
 // Avito tab activation, bounded snapshot moment and return transition.
@@ -18,7 +18,7 @@
 const ensureLocks = new Map();
 const DEBUGGER_PROTOCOL_VERSION = "1.3";
 const DEBUGGER_MAX_TYPED_GRAPHEMES = 160;
-const DEBUGGER_ALLOWED_METHODS = new Set(["Input.dispatchMouseEvent", "Input.dispatchKeyEvent", "Input.insertText"]);
+const DEBUGGER_ALLOWED_METHODS = new Set(["Input.dispatchMouseEvent", "Input.dispatchKeyEvent"]);
 
 function cb(invoke) { return new Promise((resolve, reject) => invoke((value) => { const error = chrome.runtime.lastError; if (error) reject(new Error(error.message)); else resolve(value); })); }
 const storageGet = (keys) => cb((done) => chrome.storage.local.get(keys, done));
@@ -173,11 +173,17 @@
     const focus = await tabsSend(tab.id, { type: "AF_CONFIRM_DEBUGGER_TYPE_TARGET_FOCUSED", token: request.token });
     if (!focus?.ok || !focus?.data?.ok || !focus.data.intact || !focus.data.focused) throw new Error("CDP_INPUT_FOCUS_CONFIRMATION_FAILED");
     if (Number(request.previous_value_length || 0) > 0) await inputClearExistingValue(target);
-    for (const grapheme of Array.from(value)) await debuggerSend(target, "Input.insertText", { text: grapheme });
+    // Avito's city control reacts to the trusted keyboard lifecycle, not merely to
+    // a final value mutation. Generate keyDown → keypress/input → keyUp through
+    // CDP Input for every grapheme; no DOM mutation, Runtime or site API is used.
+    for (const grapheme of Array.from(value)) {
+      await inputKey(target, "keyDown", { key: grapheme, text: grapheme, unmodifiedText: grapheme });
+      await inputKey(target, "keyUp", { key: grapheme });
+    }
     const confirm = await tabsSend(tab.id, { type: "AF_CONFIRM_DEBUGGER_TYPE_VALUE", token: request.token, expected_value: value });
     if (!confirm?.ok || !confirm?.data?.ok || !confirm.data.intact || !confirm.data.focused || !confirm.data.expected_value_matches) throw new Error("CDP_INPUT_VALUE_CONFIRMATION_FAILED");
     await log("CDP_INPUT_TYPE_COMPLETED", { search_id: state.search_id, tab_id: tab.id, text_length: Array.from(value).length, target_marker: String(request.target?.marker || "").slice(0, 160), target_placeholder: String(request.target?.placeholder || "").slice(0, 160) });
-    return { ok: true, data: { ok: true, strategy: "chrome_debugger_input_mouse_focus_ctrl_a_backspace_insert_text", cdp_methods: Array.from(DEBUGGER_ALLOWED_METHODS), text_length: Array.from(value).length } };
+    return { ok: true, data: { ok: true, strategy: "chrome_debugger_input_mouse_focus_ctrl_a_backspace_trusted_key_lifecycle", cdp_methods: Array.from(DEBUGGER_ALLOWED_METHODS), text_length: Array.from(value).length } };
   } catch (error) {
     const reason = debuggerErrorCode(error);
     await log("CDP_INPUT_TYPE_FAILED", { search_id: state.search_id, tab_id: tab.id, reason, text_length: Array.from(value).length });

