# Checkpoint — восстановление после остановки на part-009

Дата: 2026-09-12. Это checkpoint восстановления, НЕ сообщение о завершённой публикации и НЕ новый runtime-патч.

## Проверенная точка GitHub

До записи этого checkpoint live `main` = `c1babd1d055a7b29de741082b1b98f350f9d1006`, tree = `89ee47c2e27c90b0980049a3240582a2ba7afc82`. Последний коммит — сохранение `part-009.b64`.

В `.github/avito-exact-archives-20260912/` присутствуют **11 из 26 частей: 000–009 и 025**. Отсутствуют **010–024**. Первый неподтверждённый файл — `part-010.b64`. Размеры и Git blob SHA каждого сохранённого файла зафиксированы в `PUBLICATION_CURSOR_2026-09-12.json`.

Наличие этих частей не означает, что ZIP опубликованы. Каталогов exact source v1.0.34/v1.0.35 и самих ZIP в проверенном main tree ещё нет. Старые workflows на 17 частей нельзя путать с текущим транспортом на 26 частей.

## Что сохранено и перепроверено локально

Оба оригинальных ZIP физически присутствуют в текущей среде. Выполнены CRC, безопасная распаковка, пересчёт размеров, SHA-256 и Git blob SHA. Исходные ZIP не изменены.

| Версия | Bytes | SHA-256 |
|---|---:|---|
| v1.0.34 | 495534 | `27e2049522663bc3d2925c7bc847ad04304979e166e8428fafaaa91d37085f5d` |
| v1.0.35 | 511868 | `3a1de9f67af1554038193adb9bf6a63f0e7ca5ef7c26690236e2fd57a287abd3` |

Полный публикационный пакет с прежним SHA-256 `bf89642eb4120fbebd70b45432ea0c6939591f0eb25527f352ae32d1b7a8ba07` в текущей среде не восстановлен. Поэтому недостающие части нельзя сочинять или получать из другого потока и добавлять к сохранённым 11. Перед продолжением загрузки нужно восстановить тот же поток и проверить полный SHA и идентичность всех уже сохранённых частей. Если необходим другой способ передачи, сохранить его как отдельный явно обозначенный транспорт, не перезаписывая этот частичный архив.

## Актуальный статус v1.0.35 — REJECTED

Прочитан уже завершённый `QA_v1.0.35_RECHECK_2026-09-12.json`: **9 групп PASS, 15 FAIL**, один environment probe не является приёмкой. Старые указания об установке v1.0.35 из первоначального checkpoint больше не действуют.

В текущем восстановлении заново исполнен настоящий Avito DOM adapter из каждого ZIP в offline Chromium:

- v1.0.34: manifest 1.0.34, фактический `AF_AVITO_PING.version` 1.0.34 — PASS, exit 0;
- v1.0.35: manifest 1.0.35, фактический `AF_AVITO_PING.version` 1.0.34 — FAIL, exit 1.

Результат: `ADAPTER_IDENTITY_RECOVERY_2026-09-12.json`. Это offline воспроизведение, не пользовательский Chrome и не новый полный агрегат.

Причина пропуска в unit-тестах подтверждена чтением `tests/helpers/worker_vm.cjs:36`: test double возвращает версию manifest, а не ответ настоящего `avito_content.js`. Он скрывает именно это расхождение. Worker-проверку равенства нельзя ослаблять.

## Точное продолжение

1. Завершить публикацию оригинальных ZIP и исходников с сохранением их байтов; v1.0.35 хранить как отклонённую.
2. Прочитать результат из GitHub обратно и сверить оба SHA-256 и версии. Workflow, staging parts или сообщение «uploaded» не заменяют эту проверку.
3. После восстановления exact-source authority исправлять доказанное расхождение в новой версии, не изменяя оригиналы v1.0.34/v1.0.35.
4. Проверить настоящий adapter PING против manifest, прежний Writing Block/zero-command контракт, полный межкомпонентный цикл и конечный ZIP. Installed/live acceptance отдельно; пока NOT_RUN.

Закрытый PR #1 от v1.0.24 не использовать. Ordinary Markdown/code block остаётся неисполняемым. Новый поиск ПК сейчас не запускать; сохранённый baseline 150/150 не сбрасывать.

## Граница сделанного в этом восстановлении

Сверены live main и архивы, заново воспроизведён version mismatch, сохранён машинный publication cursor, обновлён текущий прогресс. **Новых архивных частей не загружено; полная публикация не завершена; runtime-код не изменялся.**

## Правила и нарушения

В этом блоке соблюдены проверка фактического уровня (реальный PING), сохранность исходных ZIP, разделение offline и live, использование exact источников и фиксация промежуточного состояния. Правило 19 о завершённой Git-публикации остаётся незакрытым; это явная незавершённая работа, не PASS. Исторические нарушения v1.0.35 — незавершённая полная регрессия и публикация, неправильный unit double. Приёмка заблокирована, оригинальный FAIL не скрыт.
