# Обязательный порядок работы с Avito Finder

Перед любой работой полностью прочитать:

1. `подбор авито расширение/PATCH_ENGINEERING_RULES.md` — постоянные 19 правил.
2. `подбор авито расширение/ТЕКУЩИЙ_ПРОГРЕСС.md`.
3. `подбор авито расширение/releases/v1.0.36-r2/DELIVERY_CHECKPOINT.md`.
4. `подбор авито расширение/releases/v1.0.36-r2/PATCH_REPORT_v1.0.36_R2_RU.md`, `DELIVERY_RECEIPT.json` и `QA/verification_34696794119_1/REMOTE_READBACK.json`.

## Точная текущая точка

v1.0.36 R2 ZIP опубликован; 141 исходный файл, SHA-256/CRC, полный подтверждающий offline QA 35/35, три дополнительных popup-повтора и полный wrong-format → две команды handoff проверены. Архив SHA-256: `c703c9875687d69b2d2ab5e265268cb5fd9ef44d86a95c305a6ae24e4589104c`.

Статус: OFFLINE_QA_PASS / REMOTE_BYTES_VERIFIED / LIVE_UNVERIFIED. Следующий незакрытый этап — пользовательский установленный Chrome E2E. Не перезапускать сборку, загрузку 26 частей или исправление version mismatch: они уже завершены. Для следующего изменения сначала сверить live GitHub и конкретный пользовательский результат.

## Сохранение промежуточного результата

После каждого материального блока сохранять код, тесты, результаты и точный cursor в GitHub, проверять чтением обратно. Не оставлять единственную копию в чате или sandbox. Для частей архива сохранять алгоритм, полный манифест и хеши; не смешивать разные потоки. Для длительного QA каждый процесс должен оставлять отдельный результат и checkpoint до следующей группы. Два CI-процесса не должны одновременно писать в main.

## Отчёт каждого патча

Показывать каждое применимое правило и его фактический статус с доказательством. Отдельно писать НАРУШЕННЫЕ ПРАВИЛА. Исторические нарушения не подменять текущими; NOT_RUN не означает PASS. Проверять конечный ZIP, а не похожую рабочую папку. Не объявлять пользовательскую приёмку по offline Chromium или CI.

## Сохраняемые границы

Writing Block — единственная исполняемая assistant-команда. Обычный Markdown/code block не исполняется. Неверный формат даёт один диагностический отчёт, без Avito dispatch до корректной команды. Закрытый PR #1 от v124 не использовать. Proxy, очередь, cursor, вкладки и доставка отчётов не меняются ради метаданных версии. CAPTCHA остаётся ручной. Поиск ПК и baseline 150/150 не сбрасывать.

## История FAIL — не стирать

v135 отклонена: manifest 1.0.35 и реальный PING 1.0.34. Исправлено в v136 без ослабления worker-защиты. R1 v136 отклонён из-за зависания тестовой VM после dispose; R2 исправляет только её очистку, runtime R1/R2 одинаков.

Первый CI R2 (34695322848) имеет 34 PASS / 1 popup timeout FAIL. Он сохранён в QA/final_zip/. Причина конкретного таймаута не установлена. Новый полный прогон 34696794119 — отдельное доказательство 35 PASS / 0 FAIL, не переписывание старого FAIL. Пока пользовательский Chrome E2E не принят, релиз не называть доказанно рабочим.
